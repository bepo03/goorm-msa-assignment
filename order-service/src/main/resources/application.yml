server:
  port: 8082

spring:
  application:
    name: order-service

clients:
  catalog:
    url: http://localhost:8081

logging:
  level:
    com.example.bookstore.order.client.CatalogClient: DEBUG
